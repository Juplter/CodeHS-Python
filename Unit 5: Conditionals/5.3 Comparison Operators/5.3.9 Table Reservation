reservation_name = "CodeHS-Solutions"
user_name = input("What is your name?: ")

if (reservation_name == user_name):
    print("Right this way!")
else:
    print("Sorry, we don't have a reservation under that name.")
