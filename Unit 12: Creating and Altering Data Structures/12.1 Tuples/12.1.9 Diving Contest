# fill in this function to return the total of the three judges' scores!
def calculate_score(judge_scores):
    total_score = judge_scores[0] + judge_scores[1] + judge_scores[2]
    return total_score
