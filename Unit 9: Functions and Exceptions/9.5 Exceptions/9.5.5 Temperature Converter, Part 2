# Write your function for converting Celsius to Fahrenheit here.
# Make sure to include a comment at the top that says what
# each function does!
def celsius_to_fahrenheit(x):
    return ((1.8 * x) + 32)

# Now write your function for converting Fahrenheit to Celsius.
def fahrenheit_to_celsius(x):
    return ((x - 32) /1.8)

try:
    value = int(input("Provide a number to convert: "))
except ValueError:
    print("That isn't an integer.")

print(celsius_to_fahrenheit(value))
print(fahrenheit_to_celsius(value))
